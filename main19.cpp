#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;
int chk(vector<int>& arr){
    sort(arr.begin(),arr.end());
    for(int i = 0 ; i<arr.size() ; i++){
        for(int j = i + 1 ; j < arr.size() ; j++){
            if
        }

    }
}
int main(){
    return 0;
}